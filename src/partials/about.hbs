{{#resume.basics}}
<div class="container about-container">
    {{> title value="Adressen"}}

    {{#if location}}
        {{#location}}
        {{> info-tag text=(formatAddress address postalCode city region countryCode) icon="fa-map-marker"}}
        {{/location}}
    {{/if}}

    {{#if birthday}}
        {{> info-tag text=(concat "Geboren am " this.birthday) icon="fa-calendar"}}
    {{/if}}

    {{#if email}}
        {{> info-tag text=this.email icon="fa-envelope-o" url=(concat "mailto:" this.email)}}
    {{/if}}

    {{#if phone}}
        {{> info-tag text=this.phone icon="fa-mobile" url=(concat "tel:" (removeWhitespace this.phone))}}
    {{/if}}

    {{#if website}}
        {{> info-tag text=this.website icon="fa-globe" url=this.website}}
    {{/if}}

    {{#if profiles}}
        {{#each profiles}}
            {{> social}}
        {{/each}}
    {{/if}}
</div>
{{/resume.basics}}
