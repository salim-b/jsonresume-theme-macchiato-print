{{#if resume.work.length}}
<div class="container work-container">
    {{> title value="Work experience"}}

    {{#each resume.work}}
        <section class="item">
            {{> section-header name=this.position website=this.url }}

<<<<<<< HEAD
            {{#if position}}
            <h4>{{{markdownToHtml position}}}</h4>
||||||| a816484
            {{#if position}}
            <h4>{{position}}</h4>
=======
            {{#if name}}
            <h4>{{name}}</h4>
>>>>>>> tweaks
            {{/if}}

            {{#if location}}
            <h5 class="location">{{location}}</h5>
            {{/if}}

            {{#if keywords.length}}
            <div class="flex-container">
                {{#each keywords}}
                    <h6 class="skill">{{{markdownToHtml .}}}</h6>
                {{/each}}
            </div>
            {{/if}}

            {{#if summary}}
            <p class="summary">{{{markdownToHtml summary}}}</p>
            {{/if}}

            {{#if highlights.length}}
            <ul>
                {{#each highlights}}
                <li>{{{markdownToHtml .}}}</li>
                {{/each}}
            </ul>
            {{/if}}
        </section>
    {{/each}}
</div>
{{/if}}
